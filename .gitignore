# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor folders
.vscode/
.idea/

# Local export/scratch artifacts, if any get created again
*.zip

# Build output and dependencies
dist/
node_modules/
.eleventy-cache/

# Raw source material (personal certificates, LinkedIn exports, old-site docs) --
# already extracted into the site content; not meant to be committed or published.
dr/


# Video master: 22MB of third-party broadcast footage. The shipped encode in
# src/assets/video/ is reproducible from it (see image-src/README.md), so the
# master is kept locally but never committed.
image-src/video/
